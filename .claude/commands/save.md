---
description: Checkpoint the project so a cold session can resume without losing anything
---

Read `docs/30_bible/CHECKPOINT.md` now and follow it exactly. It is the whole
procedure and this file is deliberately a pointer to it.

**Do not reproduce the steps here.** There is a second agent entry point in this
repository — `.agents/skills/source-command-save/SKILL.md` — and when both held a
copy they had already diverged in substance: one told its agent to write the live
project state into `CLAUDE.md`, the other into `AGENTS.md`. Two documents, two
agents, one project, and neither instruction was wrong on its own terms.
`test_agent_entry_points_are_pointers.gd` refuses a copy growing back here.

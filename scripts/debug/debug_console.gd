## Tunable overrides, state visualisers, netsim.
##
## STRIPPED FROM RELEASE EXPORTS. Two of its commands earn its existence:
## `noprediction` is the only way to tell a feel bug from a prediction bug, and
## `show lagcomp` is the only practical way to diagnose a disputed kill.
##
## STUB — full console in US-0081.
extends CanvasLayer


func register(_name: String, _callable: Callable, _help: String) -> void:
	pass
